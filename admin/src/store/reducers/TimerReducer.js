const initState = {
  error : null,
 
}

 const timerReducer = (state = initState, action) =>{

    switch (action.type) {
  
      

      
        default:
            return state;
          
    }
}

export default timerReducer;